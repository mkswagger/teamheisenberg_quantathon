/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["127.0.0.1", "quest-grapgh-gen.onrender.com"], // Add any other domains as needed
  },
};

module.exports = nextConfig;
